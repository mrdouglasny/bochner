# `Sazonov.lean` — Informal Summary

> **Source**: [`Bochner/Sazonov.lean`](../../Bochner/Sazonov.lean)
> **Generated**: 2026-03-03
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

This file defines the Sazonov topology on a real Hilbert space $H$, a locally convex topology generated by seminorms $x \mapsto \sqrt{\langle x, Sx \rangle}$ indexed by positive trace-class operators $S$. A function $\varphi : H \to \mathbb{C}$ is continuous in the Sazonov topology if for every $\varepsilon > 0$ there exists a positive trace-class operator $S$ such that $\lvert\varphi(x) - \varphi(y)\rvert < \varepsilon$ whenever $\sqrt{\langle x-y, S(x-y) \rangle} < 1$. Key lemmas establish Cauchy-Schwarz for the semi-inner product $\langle \cdot, S \cdot \rangle$ and the triangle inequality for the Sazonov seminorm.

## Status
**Main result**: Fully proven (0 sorries, 0 axioms)
**Length**: 194 lines, 5 definitions + 6 theorems/lemmas

---

## Definition `IsPositiveTraceClass`
**Location**: [Line 51](../../Bochner/Sazonov.lean#L51)

An operator $S : H \to_\mathbb{R} H$ is positive trace-class if:
- $S$ is positive (self-adjoint with nonnegative spectrum)
- $\sum_i \langle e_i, S e_i \rangle$ converges for some Hilbert basis $\{e_i\}$ (this is basis-independent for positive operators)

---

## Definition `quadForm`
**Location**: [Line 59](../../Bochner/Sazonov.lean#L59)

The quadratic form associated to an operator $S$ is $x \mapsto \langle x, Sx \rangle$.

---

## Lemma `quadForm_nonneg`
**Location**: [Line 69](../../Bochner/Sazonov.lean#L69)

If $S$ is positive, then $\langle x, Sx \rangle \geq 0$ for all $x$.

Proof uses: positivity of $S$ gives $\text{Re}(\langle x, Sx \rangle) \geq 0$; for real inner products this is the full value.

---

## Lemma `inner_S_smul_right`
**Location**: [Line 75](../../Bochner/Sazonov.lean#L75)

$\langle x, S(t \cdot y) \rangle = t \langle x, Sy \rangle$ (linearity in scalar multiplication on the right).

Proof uses: linearity of $S$ and bilinearity of the inner product.

---

## Lemma `quadForm_smul`
**Location**: [Line 79](../../Bochner/Sazonov.lean#L79)

$\langle t \cdot x, S(t \cdot x) \rangle = t^2 \langle x, Sx \rangle$ (homogeneity of degree 2).

Proof uses: bilinearity of the inner product and linearity of $S$.

---

## Lemma `quadForm_add`
**Location**: [Line 84](../../Bochner/Sazonov.lean#L84)

$\langle x+y, S(x+y) \rangle = \langle x, Sx \rangle + 2\langle x, Sy \rangle + \langle y, Sy \rangle$ (polarization identity).

Proof uses: bilinearity of the inner product and the fact that $S$ is self-adjoint (so $\langle y, Sx \rangle = \langle x, Sy \rangle$).

---

## Lemma `inner_sq_le_quadForm_mul` (Cauchy-Schwarz)
**Location**: [Line 96](../../Bochner/Sazonov.lean#L96)

If $S$ is positive, then $\langle x, Sy \rangle^2 \leq \langle x, Sx \rangle \cdot \langle y, Sy \rangle$ (Cauchy-Schwarz for the semi-inner product $\langle \cdot, S \cdot \rangle$).

Proof uses: the quadratic $t \mapsto \langle x + ty, S(x + ty) \rangle$ is nonnegative for all $t$, so its discriminant (as a quadratic in $t$) is nonpositive. Cases on whether $\langle y, Sy \rangle = 0$ or is positive.

---

## Lemma `sqrt_quadForm_add_le` (Triangle Inequality)
**Location**: [Line 140](../../Bochner/Sazonov.lean#L140)

$\sqrt{\langle x+y, S(x+y) \rangle} \leq \sqrt{\langle x, Sx \rangle} + \sqrt{\langle y, Sy \rangle}$ (triangle inequality for the Sazonov seminorm).

Proof uses: `quadForm_add` to expand the left side, `inner_sq_le_quadForm_mul` to bound the cross term, and algebraic manipulation.

---

## Definition `sazonovSeminorm`
**Location**: [Line 161](../../Bochner/Sazonov.lean#L161)

The Sazonov seminorm for a positive operator $S$ is $x \mapsto \sqrt{\langle x, Sx \rangle}$.

This is a seminorm because:
- Maps 0 to 0
- Subadditive (by `sqrt_quadForm_add_le`)
- Negation invariant
- Homogeneous: $p(a \cdot x) = \lvert a \rvert \cdot p(x)$

Proof uses: `quadForm_smul`, `Real.sqrt_mul`, and `Real.sqrt_sq_eq_abs`.

---

## Structure `SazonovIndex`
**Location**: [Line 176](../../Bochner/Sazonov.lean#L176)

The index type for Sazonov seminorms: consists of a positive trace-class operator `op` bundled with its positivity `pos` and trace-class property `traceClass`.

---

## Definition `sazonovFamily`
**Location**: [Line 183](../../Bochner/Sazonov.lean#L183)

The Sazonov seminorm family indexed by `SazonovIndex H`: maps each positive trace-class operator $S$ to its Sazonov seminorm $x \mapsto \sqrt{\langle x, Sx \rangle}$.

---

## Definition `sazonovTopology`
**Location**: [Line 189](../../Bochner/Sazonov.lean#L189)

The Sazonov topology on $H$ is the locally convex topology generated by the seminorm family `sazonovFamily H` (via the module filter basis construction).

---

*This file has **5** definitions and **6** theorems/lemmas.*
